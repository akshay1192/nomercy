syntax = "proto3";

package proto;

message Request {
    int64 num = 1;
}

message Response {
    int64 result = 2;
}

service TestService {
    rpc GetMax(stream Request) returns (stream Response);
}
